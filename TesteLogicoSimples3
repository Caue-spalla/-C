#include <stdio.h>

int main(void){
    int i = 3;
    float som = 0;
    while(i < 121){
        som = som + (i / (i + 6.0));
        i++;
    } 
    printf("O resultado da soma é: %f \n", som);
    return 0;
}
